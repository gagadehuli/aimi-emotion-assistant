# 前端非敏感配置。复制为 .env 后再修改。永远不要在这里放任何 API key。
# Expo 会把所有 EXPO_PUBLIC_* 注入到客户端 bundle，等于公开。

# 你本地后端的局域网地址。例如 http://192.168.31.42:8787
# 没配置 → App 直接走 mock 回复，不发任何网络请求。
EXPO_PUBLIC_AI_PROXY_URL=

# proxy 或 mock。proxy 时会请求 EXPO_PUBLIC_AI_PROXY_URL；mock 强制走本地随机回复。
EXPO_PUBLIC_AI_PROVIDER=mock
